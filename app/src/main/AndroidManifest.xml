<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="wangwn">

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@drawable/bg_xl"
        android:label="@string/myappname"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Guide">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity">

        </activity>
        <activity android:name=".ChooseRoom" />
        <activity android:name=".ChooseNoofPerson" />
        <activity android:name=".multiPeople" />
        <activity android:name=".SuccessActivity" />
        <activity android:name=".BacktoSuccessActivity"></activity>
    </application>

</manifest>